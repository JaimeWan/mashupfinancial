#注册中心地址
eureka:
  client:
    service-url:
     defaultZone: http://localhost:8761/eureka/
server:
  port: 8765
#  定义服务名
spring:
  application:
    name: service-feign
#    开启断路器
feign:
   hystrix:
     enabled: true