spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri:
          search-paths: 
          default-label: master
          username:
          password:
server:
  port: 8888